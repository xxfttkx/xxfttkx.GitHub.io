---
title: "20260103"
description: 
date: 2026-01-03T17:57:35+08:00
comments: true
draft: false
tags: ["diary"]
---
0216睡到0918，又躺了一天，好舒服。

想写chaldea的编队json转成fga能用的json发现chaldea的导出json是关卡json不是行动json，我没招了。

去看看chaldea代码吧，

```
flutter upgrade
flutter pub get
```

chaldea的全面屏从左右边滑动后退有UI异常，不知道是我手机问题还是什么问题，看看能不能改。

> 全面屏手势 ≠ Flutter 自己的 Navigator.pop

有奇怪的dart语法`    this.galleryIconSize = .medium,`，运行不了。投降喵投降喵。发现是enum省略的写法，是我该升级dart吗？？

> flutter升级了就够了，不用单独升级dart，这里是编辑器的问题，运行 `Dart: Restart Analysis Server` 就行了。

磁盘空间不足。C盘156MB可用，怎么又到了这一步。

```
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle
setx GRADLE_USER_HOME F:\gradle
```

搬家啦。Pub cache也能搬。

```
setx PUB_CACHE F:\pub-cache
echo $env:PUB_CACHE
Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Pub\Cache"
```

下次再说吧。

`Flutter: Running Gradle task 'assembleDebug'...`构建了一辈子还出了很多错，好冷，明天再说了。

安卓构建失败，不管了，windows能debug就行。那也明天再看代码了。
