---
title: "20260208"
description: 
date: 2026-02-08T17:33:10+08:00
comments: true
draft: false
tags: ["diary"]
---
0242睡到1000，这波不该熬夜的。

安装ComfyUI失败，不得不update `英特尔®第 11 – 14 代处理器显卡 - Windows*`。然后C盘又挑挑拣拣要删一些，唉。其实C盘有很多能删，但有点点点点舍不得，有点一年甚至只用一次，该删的，要用再下就是了。

安装驱动总感觉有点恐怖，搞不好说不定电脑直接就报废了。

重启完电脑算是无事发生，只不过笔记本屏幕闪来闪去，有点吓人，锁帧率从60Hz改到165Hz就不闪了，不知道为什么。

更新了仍然提示

```
Your NVIDIA driver may be too old for PyTorch 2.10.0+cu130.

Detected driver version: 556.12
Recommended minimum: 580

Please consider updating your NVIDIA drivers and retrying if you run into issues.
```

我没招了。

我有招的，我一看log就发现不是驱动的问题。

然而还是有问题，用不了CUDA。`把 PyTorch 从 cu130 → cu121`。

`ERROR: Could not install packages due to an OSError: [Errno 28] No space left on device`无言了，怒删famzagames，之前看到移出C盘的教程一直懒得弄，直接卸了得了。

```
(ComfyUI) PS F:\Others\ComfyUI\.venv\Scripts> python
Python 3.12.11 (main, Aug 18 2025, 19:17:54) [MSC v.1944 64 bit (AMD64)] on win32
Type "help", "copyright", "credits" or "license" for more information.
>>> import torch
>>> print("torch:", torch.__version__)
torch: 2.5.1+cu121
>>> print("cuda available:", torch.cuda.is_available())
cuda available: True
>>> print("torch cuda:", torch.version.cuda)
torch cuda: 12.1
>>> print("gpu:", torch.cuda.get_device_name(0) if torch.cuda.is_available() else "N/A")
gpu: NVIDIA GeForce RTX 3060 Laptop GPU
```

唉3060。世人诋毁你，只有我知道你曾经的辉煌。

`python -m pip freeze > F:\Others\ComfyUI\requirements.lock.txt`才知道还能这样锁版本。

能用了，但我依旧生成一堆妖魔鬼怪。
